/**
 * Returns a move by bot. (a card in hand)
 * board[][] has two possible values null or {id: playerId} representing a coin.
 * hand[] is array of cards in current hand.
 */
export default function makeMove(board, hand) {}
